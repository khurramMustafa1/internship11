import 'package:flutter/material.dart';
class comment_recent extends StatelessWidget {
  const comment_recent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
